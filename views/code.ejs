<div>
    <form action="/code/save/?Id=<%= project._id %>&contentId=<%= code._id %>" method="POST">
        <textarea name="content"  cols="30" rows="10"><%= code.content %></textarea>
        <input type="submit" value="Save">
    </form>
</div>